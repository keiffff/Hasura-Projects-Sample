{
  "name": "hasura-sample-project",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "frontend": "yarn workspace frontend",
    "backend": "yarn workspace backend"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "packages/*"
    ]
  }
}
